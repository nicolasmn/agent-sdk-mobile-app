/**
 * Bloop Relay — always-on bridge between Letta Cloud and the mobile app.
 *
 * Maintains Agent SDK sessions for user conversations, streams events,
 * and sends Expo push notifications when the mobile app is backgrounded.
 * Handles tool call approvals and context input relayed from notification actions.
 *
 * MVP scope: basic push notifications for new assistant messages when backgrounded.
 * Extended: live step updates (Live Activities), actionable notifications (accept/deny,
 * context input).
 */

// TODO: implement in NIC-6
console.log("bloop-relay: not yet implemented");
